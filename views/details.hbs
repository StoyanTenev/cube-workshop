{{> header}}

<h1>{{cube.name}}</h1>
<img class="cube" src={{cube.imageUrl}}>
<div class="details">
    <p><span>Description:</span>{{cube.description}}</p>
    <p><span>Difficulty level:</span>{{cube.difficulty}}</p>
    <a class="btn" href="/">Back</a>
    {{#if isAuth}}
        <a class="btn" href="/edit/{{cube.id}}">Edit</a>
        <a class="btn" href="/delete/{{cube.id}}">Delete</a>
        <a class="btn" href="/attach/accessory/{{cube.id}}">Attach Accessory</a>
    {{/if}}

</div>
<h2>Accessories</h2>
<div class="accessories">
    {{#if accessories}}
        {{#each accessories}}
            <div class="accessory">
                <img src={{imageUrl}} alt="stickerName">
                <h3>{{name}}</h3>
                <p>{{description}}</p>
            </div>
        {{/each}}
    {{else}}
        <h3 class="italic">This cube has no accessories yet...</h3>
    {{/if}}
</div>

{{> footer}}